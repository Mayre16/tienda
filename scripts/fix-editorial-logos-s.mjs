/**
 * DESHABILITADO — no ejecutar.
 *
 * Este script pintó una "S" (Montserrat Black) encima del identificador
 * Editorial Logos; en pantalla se veía más gruesa/negrita que el resto
 * de «LOGOS». El recorte se corrige con CSS en EditorialSiteHeader
 * (overflow:visible; sin margin negativo del nav).
 *
 * Assets restaurados desde git el 2026-07-31.
 */
console.error(
  "fix-editorial-logos-s.mjs está deshabilitado. No repintes la S del logo.",
);
process.exit(1);
